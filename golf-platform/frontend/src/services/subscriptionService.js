import api from './api';

export const subscriptionService = {
    // Scaffolded for future Subscription Controller endpoints
    getSubscriptionStatus: async (userId) => {
        // const response = await api.get(`/subscriptions/status/${userId}`);
        // return response.data;
        return null;
    }
};
