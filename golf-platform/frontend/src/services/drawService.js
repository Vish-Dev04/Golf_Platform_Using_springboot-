import api from './api';

export const drawService = {
    // Scaffolded for future Draw Controller endpoints
    getAllDraws: async () => {
        // const response = await api.get('/draws');
        // return response.data;
        return [];
    }
};
